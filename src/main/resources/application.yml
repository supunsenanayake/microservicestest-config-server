server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///${user.dir}\config-server\config-server\src\main\config-repo   #C:\Users\supun\Spring_Test_code\2025\04\06\config-server\config-server\src\main\config-repo
          clone-on-start: true
          default-label: main

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
